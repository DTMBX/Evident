name: Dependency Security Scan

on:
  push:
    branches: [ main, develop, copilot/** ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    # Run weekly on Mondays at 9 AM UTC
    - cron: '0 9 * * 1'
  workflow_dispatch:

jobs:
  python-security-scan:
    name: Python Dependency Scan
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install pip-audit
        run: |
          python -m pip install --upgrade pip
          pip install pip-audit==2.7.3
      
      - name: Scan backend dependencies
        run: |
          pip-audit -r backend/requirements.txt --format json --output governance/vulnerabilities-backend.json || echo "Vulnerabilities found"
      
      - name: Check for HIGH/CRITICAL vulnerabilities
        run: |
          if [ -f governance/vulnerabilities-backend.json ]; then
            # Parse JSON and check for HIGH or CRITICAL severity
            HIGH_COUNT=$(jq '[.vulnerabilities[] | select(.severity == "HIGH" or .severity == "CRITICAL")] | length' governance/vulnerabilities-backend.json)
            
            if [ "$HIGH_COUNT" -gt 0 ]; then
              echo "❌ Found $HIGH_COUNT HIGH/CRITICAL vulnerabilities"
              jq '.vulnerabilities[] | select(.severity == "HIGH" or .severity == "CRITICAL") | {package: .name, version: .version, id: .id, severity: .severity}' governance/vulnerabilities-backend.json
              exit 1
            else
              echo "✅ No HIGH/CRITICAL vulnerabilities found"
            fi
          fi
      
      - name: Upload vulnerability report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: vulnerability-report
          path: governance/vulnerabilities-backend.json
          retention-days: 90
      
      - name: Comment on PR (if vulnerabilities found)
        if: failure() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let vulns = [];
            
            if (fs.existsSync('governance/vulnerabilities-backend.json')) {
              const data = JSON.parse(fs.readFileSync('governance/vulnerabilities-backend.json', 'utf8'));
              vulns = data.vulnerabilities.filter(v => 
                v.severity === 'HIGH' || v.severity === 'CRITICAL'
              );
            }
            
            if (vulns.length > 0) {
              const body = `## ⚠️ Security Alert: Vulnerabilities Found
            
            Found **${vulns.length}** HIGH/CRITICAL vulnerabilities in Python dependencies:
            
            ${vulns.map(v => `- **${v.name}** ${v.version}: ${v.id} (${v.severity})`).join('\n')}
            
            Please review and update affected dependencies before merging.
            
            Full report available in workflow artifacts.`;
              
              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: body
              });
            }

  sbom-generation:
    name: Generate SBOM
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install CycloneDX
        run: |
          python -m pip install --upgrade pip
          pip install cyclonedx-bom==4.6.4
      
      - name: Generate SBOM for backend
        run: |
          mkdir -p governance
          cyclonedx-py requirements backend/requirements.txt \
            --output governance/sbom-backend.json \
            --format json
      
      - name: Validate SBOM
        run: |
          # Check SBOM was generated
          if [ ! -f governance/sbom-backend.json ]; then
            echo "❌ SBOM generation failed"
            exit 1
          fi
          
          # Count components
          COMPONENT_COUNT=$(jq '.components | length' governance/sbom-backend.json)
          echo "✅ SBOM generated with $COMPONENT_COUNT components"
          
          if [ "$COMPONENT_COUNT" -lt 20 ]; then
            echo "⚠️ Warning: Expected more components (found $COMPONENT_COUNT)"
          fi
      
      - name: Upload SBOM
        uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: governance/sbom-backend.json
          retention-days: 90
      
      - name: Commit SBOM (main branch only)
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          if [ -f governance/sbom-backend.json ]; then
            git add governance/sbom-backend.json
            git diff --staged --quiet || git commit -m "chore: Update SBOM [skip ci]"
            git push
          fi

  license-check:
    name: License Compliance Check
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install pip-licenses
        run: |
          pip install pip-licenses==4.5.3
      
      - name: Check licenses
        run: |
          # Install dependencies first
          pip install -r backend/requirements.txt
          
          # Generate license report
          pip-licenses --format=json --output-file=governance/licenses.json
          
          # Check for GPL (copyleft) licenses
          GPL_COUNT=$(jq '[.[] | select(.License | contains("GPL") and (contains("LGPL") | not))] | length' governance/licenses.json)
          
          if [ "$GPL_COUNT" -gt 0 ]; then
            echo "⚠️ Warning: Found $GPL_COUNT GPL-licensed dependencies"
            jq '.[] | select(.License | contains("GPL") and (contains("LGPL") | not)) | {Name, Version, License}' governance/licenses.json
          else
            echo "✅ No GPL-licensed dependencies found"
          fi
      
      - name: Upload license report
        uses: actions/upload-artifact@v4
        with:
          name: license-report
          path: governance/licenses.json
          retention-days: 90

  summary:
    name: Security Summary
    runs-on: ubuntu-latest
    needs: [python-security-scan, sbom-generation, license-check]
    if: always()
    
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        continue-on-error: true
      
      - name: Generate summary
        run: |
          echo "# 🔒 Dependency Security Scan Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Date**: $(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Vulnerability scan results
          if [ -f vulnerability-report/vulnerabilities-backend.json ]; then
            VULN_COUNT=$(jq '.vulnerabilities | length' vulnerability-report/vulnerabilities-backend.json)
            echo "## 🛡️ Vulnerability Scan" >> $GITHUB_STEP_SUMMARY
            echo "- Total vulnerabilities: **$VULN_COUNT**" >> $GITHUB_STEP_SUMMARY
            
            if [ "$VULN_COUNT" -eq 0 ]; then
              echo "- Status: ✅ **PASS** - No vulnerabilities found" >> $GITHUB_STEP_SUMMARY
            else
              HIGH_COUNT=$(jq '[.vulnerabilities[] | select(.severity == "HIGH" or .severity == "CRITICAL")] | length' vulnerability-report/vulnerabilities-backend.json)
              if [ "$HIGH_COUNT" -gt 0 ]; then
                echo "- Status: ❌ **FAIL** - $HIGH_COUNT HIGH/CRITICAL vulnerabilities" >> $GITHUB_STEP_SUMMARY
              else
                echo "- Status: ⚠️ **WARNING** - $VULN_COUNT low/medium vulnerabilities" >> $GITHUB_STEP_SUMMARY
              fi
            fi
          fi
          
          # SBOM results
          if [ -f sbom/sbom-backend.json ]; then
            COMPONENT_COUNT=$(jq '.components | length' sbom/sbom-backend.json)
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "## 📦 Software Bill of Materials" >> $GITHUB_STEP_SUMMARY
            echo "- Components tracked: **$COMPONENT_COUNT**" >> $GITHUB_STEP_SUMMARY
            echo "- Status: ✅ SBOM generated successfully" >> $GITHUB_STEP_SUMMARY
          fi
          
          # License check results
          if [ -f license-report/licenses.json ]; then
            LICENSE_COUNT=$(jq '. | length' license-report/licenses.json)
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "## ⚖️ License Compliance" >> $GITHUB_STEP_SUMMARY
            echo "- Licenses checked: **$LICENSE_COUNT**" >> $GITHUB_STEP_SUMMARY
            echo "- Status: ✅ License check complete" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "*Automated scan by GitHub Actions*" >> $GITHUB_STEP_SUMMARY

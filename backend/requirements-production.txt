# Production Requirements for Render.com
# Excludes heavy AI dependencies that cause build timeouts

# Core Flask Framework
Flask==3.1.2
Flask-CORS==5.0.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-Bcrypt==1.0.1
Werkzeug==3.1.3

# Production Server
gunicorn==23.0.0

# Database
psycopg2-binary==2.9.11
SQLAlchemy==2.0.36

# Core Dependencies
python-dotenv==1.0.1
requests==2.32.5
Pillow==11.0.0
pypdf==5.1.0
pdfplumber==0.11.4
cryptography==44.0.0
certifi==2024.12.14

# AI & External Services (lightweight)
openai==2.15.0              # OpenAI API client (for cloud transcription)
pytesseract==0.3.13         # OCR (uses system tesseract)
pdf2image==1.17.0           # PDF to image conversion
pyotp==2.9.0                # 2FA/TOTP authentication
qrcode[pil]==8.0            # QR code generation
stripe==11.4.0              # Payment processing

# NOTE: openai-whisper NOT included (requires ffmpeg, pytorch, causes build timeout)
# Use OpenAI API for transcription instead: https://platform.openai.com/docs/api-reference/audio
